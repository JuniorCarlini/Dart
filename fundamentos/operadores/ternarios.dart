void main() {
  //ternarios
  //complexo não é recomendado usar
  final idade = 12;

  //(condição) ? faça algo : outra coisa
  final eMaiorDeIdade = (idade >= 18 ? true : false);
  print("é maior de idade?" + eMaiorDeIdade.toString());
}